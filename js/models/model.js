/**
 * Created by iMak on 18/05/15.
 */
define([
    'underscore',
    'backbone'
], function(_, Backbone) {

    var GoodModel = Backbone.Model.extend({
        defaults: {
        }
    });

    return GoodModel;

});